# Agentic-Nexus local configuration

NEXUS_DB_PATH=data/agentic_nexus.duckdb

# setup.sh generates a random value in .env.
# Never commit the real value.
NEXUS_JWT_SECRET=replace-with-a-random-secret

# Local Ollama chat model
NEXUS_LLM_MODEL=gemma4:e4b-it

# Optional API port
PORT=8000
